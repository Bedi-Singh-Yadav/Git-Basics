$cd Desktop/git/davv_tube/
$git init #everytime you need to init repo
$git add *
$git config --global user.email "bediyadav.iips@gmail.com"
$git config --global user.email
$git config --global user.name Bedi-Singh-Yadav
$git init
$git add *
$git commit -m "Added files for Davv-Tube"
$git remote add origin https://github.com/AkankshaRathore/Davv_Tube
$git push origin master
$git push
$cd ..
$git clone https://github.com/BediSinghYadav/Git-Basics
$git add *
$cd Deskto/git/git-basics
$git add *
$git commit -m "Added file successfully"
$git push origin master
$git pull
$git help
$git help add
$git status

#if fast forward error is occur then commit all files then pull it
#
